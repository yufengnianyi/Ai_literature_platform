app:
  ai:
    pretreatment:
      artifact-root: data/rag
      output-root: PreTreatment/outputs
      prompt-path: PreTreatment/prompts/oomycete-main-study-system.txt
      max-documents: 0
      llm-max-attempts: 3
      quality:
        min-chunks: 3
        min-total-text-chars: 1500
        max-replacement-char-ratio: 0.03
        max-short-line-ratio: 0.80
      cli:
        enabled: true
        mode: scan
        apply-run-id:
        dry-run: true
