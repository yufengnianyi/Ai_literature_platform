# 本机验证记录

日期：2026-09-14。平台：Windows，Python 3.14.3，独立虚拟环境位于本目录的 `.venv`。

## 已完成

| 项目 | 结果 |
| --- | --- |
| 锁定依赖安装 | 成功；`pip check` 无依赖冲突 |
| 自动测试 | 30 项全部通过（原 26 项及新增 4 项离线诊断测试） |
| 三个命令 | `prepare`、`run --mode both`、`evaluate` 均完成离线验证 |
| 模拟 A/B | `outputs/demo-run-verified`，状态 `COMPLETED`，明确标记 `SIMULATED` |
| 来源与失败处理 | 引用错配、非法成员引用、缺摘要、无问题、失败和断点续跑已有自动测试 |
| 人工审阅保护 | 未修改条目的审阅值在重跑后保留；源字段被篡改会拒绝评价 |
| 评价分母 | 删除审阅行仍保留待审；有参考问题但无输出的文献计入覆盖分母 |
| 密钥缺失检查 | 清晰拒绝真实运行，未发起真实模型请求 |

执行测试：

```powershell
.\problem_discovery\.venv\Scripts\python.exe -m unittest discover -s problem_discovery/tests -v
```

测试还覆盖缓存和提示词/输入/模型变化、损坏缓存、重复原句偏移消歧、向量顺序与有效性、空集合和单例，以及认证失败后立即停止整次运行。用于测试的网络响应和向量均为模拟。

## 模拟运行产物

- 输入：五篇有摘要的虚构记录，另有一篇缺摘要记录被单列。
- A：两个有共同问题的组，以及一个保留的无问题单例组。
- B：四篇各提炼一个问题，归为两组；另一篇保留 `INSUFFICIENT_INFORMATION`。
- 共执行 12 次模拟调用，真实提供商调用为零；最终模拟运行禁用缓存。
- 运行报告：`outputs/demo-run-verified/report.md`。
- 人工审阅表：`outputs/demo-run-verified/review.csv`。
- 评价报告：`outputs/demo-run-verified/evaluation.md`，尚未填写的评价为 `PENDING`。

上述数量只说明软件输出与模拟数据相符，不代表真实文献的准确率或聚类质量。早期 `demo-run` 是调试产物；最终核验对应 `demo-run-verified`。

## 真实文献数据准备

使用现有 `data/rag`，读取原始 TEI 摘要并按固定种子 42 抽样：

| 数据状态 | 数量 |
| --- | ---: |
| 扫描目录 | 1,300 |
| 去重及摘要检查后可用 | 1,054 |
| 缺少所需 TEI 产物 | 138 |
| 缺少可用摘要 | 55 |
| 保守去重排除 | 53 |
| 实际选择 | 10 |

已准备的输入：`outputs/real-prepared-10/corpus.jsonl`。来源、哈希及抽样记录位于同目录的 `manifest.json`，未选中和排除原因位于 `excluded.jsonl`。

## 十篇真实模型运行

2026-09-14 已将项目现有凭据临时注入运行进程，完成真实 A/B 实验；没有修改持久环境变量或模型代码。

- 运行目录：`outputs/real-run-10-20260914`，状态 `COMPLETED`，提供商 `DASHSCOPE`。
- Chat：`qwen3-max-2026-01-23`；Embedding：`text-embedding-v4`，1024 维；距离阈值 `0.35`。
- A：10 个组，24 个目录问题，9 篇有目录问题，1 篇无目录问题。
- B：10 篇提炼 27 个问题，聚为 13 个组，生成 27 个目录问题。
- 两组跨文献聚类数量均为 0；B 的多成员组只合并了同一篇的不同问题。
- 40 次真实提供商请求，无缓存命中；1 次引用错配校验失败，自动重试后通过，无最终技术失败。
- 提供商报告累计 59,057 tokens；API 调用累计耗时约 230 秒，未计算货币费用。
- 10 篇摘要逐一核对本地 TEI 均一致；79 处抽取及目录证据偏移核对通过，目录证据成员引用均属于对应组。
- 19 个预期输出文件齐全；`evaluate` 已执行，158 行人工审阅表尚未填写。

中文解读见 `outputs/real-run-10-20260914/summary.zh.md`。原始响应存在术语翻译错误、同篇相近问题拆散等质量问题；没有为改善观感改写实验输出。

## 尚未完成

同日完成离线阈值扫描及助手初步审阅，未新增 API 调用。诊断输出：`outputs/real-run-10-diagnostics-20260914`。0.55 时 A/B 分别出现 2/3 个跨文献组，但存在语义混合风险；0.65 时 A 将全部十篇合为一组，B 有一个包含九篇的组。没有据此修改默认阈值或给出最佳方案结论。

专家语义支持、遗漏、归组合理性和目录修改量评价尚未完成，状态为 `PENDING_EXPERT_DECISION`。真实运行通过不等于科学验收通过；本次结果尚不能证明发现了领域级的跨文献共同问题，也不能据此判定 B 优于 A。

## 一百篇真实模型运行

2026-09-14 使用同一模型、Prompt、种子 42 和阈值 0.35，将实验扩展到 100 篇。十篇基线均包含在新快照中。

- 输入：`outputs/real-prepared-100-20260914`，100 篇，语料哈希 `b4957573f68661b6bca985a88ca139f5ebe7e30db0f4646f1f1415744f8937c2`。
- 运行：`outputs/real-run-100-20260914`，状态 `PARTIAL`。
- A：90 组，83 个单例，7 个跨文献组覆盖 17 篇；150 个目录问题；12 个最终归纳失败组，36 个成员没有目录问题覆盖。
- B：提取 229 个问题，87 篇成功、12 篇信息不足、1 篇提取失败；100 组，31 个单成员组，6 个跨文献组覆盖 13 篇；210 个目录问题，无最终归纳或向量失败。
- 调用：343 次真实请求，30 次缓存命中，49 次失败尝试，累计 514,243 tokens；没有认证、欠费、限流或向量接口错误。
- 核验：100 篇摘要与 TEI 一致；634 处已接受证据和成员引用全部匹配；19 类产物齐全；审阅表 1,308 行；自动测试 30 项通过。
- 输入质量：2 篇标题为空，5 篇标题为出版流程模板文本，另有 OCR/断词污染。
- 离线阈值诊断：`outputs/real-run-100-diagnostics-20260914`，未新增模型调用，也未据此宣布最佳阈值。

中文结果和初步解读见 `outputs/real-run-100-20260914/summary.zh.md`。专家评价仍未完成，人工指标保持为空。
