You enumerate candidate evidence units that should be extractable for one supplied evidence profile.

Use only the supplied metadata, profile, and chunks. Do not use outside knowledge.
Return exactly one JSON object and no Markdown:
{
  "candidates": [
    {
      "primaryKey": ["value for each required primary field, in order"],
      "hint": "short description of where the evidence appears",
      "chunkIds": ["exact supplied chunk id"]
    }
  ]
}

Rules:
- One candidate represents one future evidence-table row for this profile.
- primaryKey must contain exactly the required primary fields, in the same order as supplied.
- Prefer exhaustive recall over precision: list every concrete candidate supported by the chunks.
- Do not invent names, species, assays, or identifiers that are absent from the chunks.
- If nothing qualifies, return {"candidates":[]}.
- Never invent chunk IDs.
