Extract evidence for exactly one supplied evidence profile from one scientific paper and summarize it as a Markdown table.

Use only the supplied metadata and chunks. Do not use outside knowledge. Do not invent missing information. Preserve scientific names, identifiers, numeric values, units, primer sequences, mutations, gene names, strain names, and DOI values exactly as reported. If a field is absent or uncertain, leave that cell empty. Do not write "N/A", "not reported", "unknown", "未报道", "未知", or similar placeholders.

Output exactly one Markdown table and no explanatory text. The table must use the supplied headers in the exact order. Include the header row and one separator row. If the supplied paper contains no qualifying evidence, return only the required header row and separator row with no data rows.

One row must follow the supplied evidence profile's row unit and split rules. Create separate rows when the supplied split rules require separation. If multiple values belong to the same field in the same row, keep them in the same cell separated by commas or semicolons.

Field guidance:
- Follow the supplied profile scope, field guidance, headers, and required primary fields.
- Fill only values supported by the supplied paper content.
- Do not cite one paper's bibliography as evidence for claims made only by another paper.
- For review-derived entries, extract only concrete evidence explicitly tied to original studies when the supplied profile allows review evidence, and mark the limitation in Notes.

Tables and codes:
- Table captions, headers, footnotes, body rows, and nearby body explanations may all contain key information.
- Chunks whose section starts with "Table" contain a Markdown table rendered from the paper. Read each data row as candidate evidence when it matches the supplied profile.
- Chunks labeled as Recovered Table or table_body_recovered are synthetic Markdown reconstructions of table bodies that were flattened in the paper text. Treat them as table evidence, still cross-checking their caption, footnotes, and method context from the supplied chunks.
- Table cells often use codes: footnote markers, compound numbers, gene IDs, strain abbreviations, species abbreviations, or other short labels. Resolve codes using table footnotes, legends, captions, nearby text, or methods/results text when explicitly provided.
- If a code cannot be resolved from the supplied chunks, keep the original code rather than inventing an expansion.
