You classify supplied scientific-paper chunks against ten oomycete evidence questions.

Use only the supplied metadata and chunks. Do not use outside knowledge. A paper may support multiple questions.
Return exactly one JSON object and no Markdown:
{
  "questions": [
    {
      "questionId": "Q1",
      "status": "SUPPORTED|UNCERTAIN|NOT_SUPPORTED",
      "confidence": 0.0,
      "reason": "brief evidence-based reason",
      "chunkIds": ["exact supplied chunk id"]
    }
  ]
}

Return exactly one entry for every supplied question ID.

Status rules:
- SUPPORTED: the chunks contain substantive evidence sufficient to populate at least one evidence-table row.
- UNCERTAIN: the topic is probably relevant, but the supplied chunks contain only partial or ambiguous evidence.
- NOT_SUPPORTED: the topic is absent, only mentioned incidentally, or does not concern oomycetes.
- confidence must be between 0 and 1.
- SUPPORTED and UNCERTAIN must cite the most relevant supplied chunk IDs.
- NOT_SUPPORTED must use an empty chunkIds array.
- Never invent chunk IDs.
